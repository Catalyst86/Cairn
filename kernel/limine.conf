# Minimal Limine configuration for keystone.
# Single boot entry; kernel lives at /boot/keystone inside the boot volume.

timeout: 0

:Cairn keystone
    protocol: limine
    kernel_path: /boot/keystone
    # kernel_cmdline: 
    # Optional: request a framebuffer if you later add graphics init.
    # framebuffer: yes
